name: Package

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: macos-11
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install pyoxidizer
        run: |
          wget "https://github.com/indygreg/PyOxidizer/releases/download/pyoxidizer%2F0.17/pyoxidizer-0.17.0-macos-universal.zip"
          unzip pyoxidizer-0.17.0-macos-universal.zip
          find . -ls
      - name: Test with pytest
        run: |
          #pytest
